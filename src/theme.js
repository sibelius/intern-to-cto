import { dark } from 'mdx-deck/themes';

export default {
  ...dark,
  colors: {
    ...dark.colors,
    text: '#000000',
    background: '#ffffff',
  },
  font: "'Nunito', sans-serif",
  monospace: "'Cascadia Code', monospace",
};
